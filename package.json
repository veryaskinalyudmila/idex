{
  "name": "idex",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ veryaskinalyudmila/idex",
    "create-local": "graph create --node http://localhost:8020/ veryaskinalyudmila/idex",
    "remove-local": "graph remove --node http://localhost:8020/ veryaskinalyudmila/idex",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 veryaskinalyudmila/idex"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
